import React, { Component } from 'react'//rcc permet directement la structure

export default class Testa extends Component { //export default permet d'exporter directement
    render() {
        return (
            <div>
                <h2>Mon deuxième composant</h2>
            </div>
        )
    }
}

